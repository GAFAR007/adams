# Agent Instructions

- After completing code changes, push and deploy when repository access and the deployment workflow are available.
- If pushing or deploying is not performed directly, provide the exact terminal commands needed to push the branch and trigger the deployment path.
- The frontend deploys through GitHub Actions on pushes to `main` via `.github/workflows/deploy-web.yml`.
